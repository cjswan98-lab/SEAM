[build]
  publish = "public"
  command = ""

[functions]
  node_bundler = "esbuild"
  directory = "netlify/functions"

# Long cache on the manifest, none on the app shell.
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache"
